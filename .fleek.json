{
    "build": {
      "image": "node:16.19",
      "command": "yarn && yarn build && yarn export",
      "publicDir": "dist",
      "environment": {
        "SERVER_ENV": "production"
      }
    }
}
