{
    "build": {
      "buildCommand": "yarn && yarn build && yarn export",
      "dockerImage": "node:lts",
      "publishDirectoryPath": "dist",
      "environment": {
        "SERVER_ENV": "production"
      }
    }
}
